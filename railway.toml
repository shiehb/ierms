[build]
builder = "nixpacks"

[deploy]
startCommand = "cd /app/server && . /opt/venv/bin/activate && python manage.py migrate && /opt/venv/bin/gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120"